apiVersion: v1
kind: Service
metadata:
  name: frontend-service
spec:
  type: NodePort
  selector:
    app: streamlit-web
  ports:
  - name: web
    protocol: TCP
    port: 80
    targetPort: 8501
    nodePort: 30085
